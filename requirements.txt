# Core analysis backends
anthropic            # Claude backend (ANALYSIS_BACKEND=claude)
google-genai>=2.9   # Gemini native-video backend + Deep Research agent (interactions API). Rollback: pip install "google-genai==1.15.0" (disables Deep Research, rest still works)
truststore           # trust the OS cert store (corporate VPN/firewall HTTPS inspection)

# Audio capture (system loopback + mic) on Windows
pyaudiowpatch

# Local transcription (Claude/local backends)
faster-whisper

# Semantic-search knowledge base (vectors + cosine search). Embeddings use the
# Gemini API by default; numpy stores/compares the vectors locally.
numpy
# Optional offline embedding fallback (BAAI/bge-small-en-v1.5). Not installed by
# default — `pip install fastembed` only if you want the local backend.
# fastembed

# System-tray icon (run minimized in the background)
pystray
pillow

# Note: ffmpeg/ffprobe must be on PATH or placed in ./ffmpeg/.
# The GUI uses tkinter (bundled with Python). Python 3.10+ recommended.
